# CWE Rule Files

CWE rule files describe source, sink, and trace matching behavior outside Python code.

- `CWE78.yaml`: supported MVP rule for OS command injection.
- `CWE134.yaml`: partially supported next-step rule for uncontrolled format strings.

Unsupported discovered CWEs should be recorded and skipped according to `analysis.unsupported_cwe_policy`.

